/**
 * Shared constants across game modules.
 */

export const CELL_TYPES = {
    EMPTY: 0,
    WALL: 1,
    HOLE: 2,
    GOAL: 3
};

export const MAX_LIVES = 3;

export const STORAGE_KEY = 'tilt-maze-progress';
